import { createMuiTheme } from "@material-ui/core";

const mainTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#2ce198',
    },
    secondary: {
      main: '#ff9800',
      light: '#e9e891',
    } 
  },
});

export default mainTheme;
